package DAY07.Set;

import java.util.HashSet;
import java.util.Set;

public class hashset {

    public static void main(String[] args) {
        HashSet<Integer> set = new HashSet<>();
        Set<String> set2 = new HashSet<>();
    }
}

/*          • Stores unique elements only.
            • No duplicates, no ordering.
            • Backed by HashMap (keys only).
            • Average O(1) operations.
*/

/*   Methods of HashSet ----
   1) .add(element) - adds element (if not already present).
   2) .remove(element) - removes element.
   3) .contains(element) - checks existence.
   4) .isEmpty() - checks if set is empty.
   5) .size() - returns size.
   6) .clear() - removes all elements.
   7) .addAll(set2) - merges two sets.
*/

